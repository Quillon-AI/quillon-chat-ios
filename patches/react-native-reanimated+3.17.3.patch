diff --git a/node_modules/react-native-reanimated/src/UpdateProps.ts b/node_modules/react-native-reanimated/src/UpdateProps.ts
index 1234567..abcdefg 100644
--- a/node_modules/react-native-reanimated/src/UpdateProps.ts
+++ b/node_modules/react-native-reanimated/src/UpdateProps.ts
@@ -115,8 +115,14 @@
           });
         },
         flush(this: void) {
-          global._updatePropsPaper!(operations);
-          operations.length = 0;
+          try {
+            global._updatePropsPaper!(operations);
+          } catch (_e) {
+            // Silently ignore prop updates for views that were unmounted during
+            // navigation or animation (iOS Paper-arch: view tag no longer valid).
+          } finally {
+            operations.length = 0;
+          }
         },
       };
     };
