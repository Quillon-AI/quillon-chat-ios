diff --git a/node_modules/react-native/React/CoreModules/RCTAlertController.mm b/node_modules/react-native/React/CoreModules/RCTAlertController.mm
index b47ce0a..343811f 100644
--- a/node_modules/react-native/React/CoreModules/RCTAlertController.mm
+++ b/node_modules/react-native/React/CoreModules/RCTAlertController.mm
@@ -11,24 +11,18 @@
 
 @interface RCTAlertController ()
 
-@property (nonatomic, strong) UIWindow *alertWindow;
+@property (nonatomic, weak) UIViewController *presentingVC;
 
 @end
 
 @implementation RCTAlertController
 
-- (UIWindow *)alertWindow
+- (UIViewController *)topmostViewController:(UIViewController *)vc
 {
-  if (_alertWindow == nil) {
-    _alertWindow = [[UIWindow alloc] initWithWindowScene:RCTKeyWindow().windowScene];
-
-    if (_alertWindow) {
-      _alertWindow.rootViewController = [UIViewController new];
-      _alertWindow.windowLevel = UIWindowLevelAlert + 1;
-    }
+  while (vc.presentedViewController && !vc.presentedViewController.isBeingDismissed) {
+    vc = vc.presentedViewController;
   }
-
-  return _alertWindow;
+  return vc;
 }
 
 - (void)show:(BOOL)animated completion:(void (^)(void))completion
@@ -41,17 +35,17 @@ - (void)show:(BOOL)animated completion:(void (^)(void))completion
 
   self.overrideUserInterfaceStyle = style;
 
-  [self.alertWindow makeKeyAndVisible];
-  [self.alertWindow.rootViewController presentViewController:self animated:animated completion:completion];
+  UIViewController *rootVC = RCTKeyWindow().rootViewController;
+  if (rootVC) {
+    _presentingVC = [self topmostViewController:rootVC];
+    [_presentingVC presentViewController:self animated:animated completion:completion];
+  }
 }
 
 - (void)hide
 {
-  [_alertWindow setHidden:YES];
-
-  _alertWindow.windowScene = nil;
-
-  _alertWindow = nil;
+  [self dismissViewControllerAnimated:NO completion:nil];
+  _presentingVC = nil;
 }
 
 @end
